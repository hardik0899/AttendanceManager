package android.hardik.attendancemanager.viewmodels.subject

import android.content.Context

import org.junit.Test
import org.junit.runner.RunWith
import org.junit.runners.JUnit4


@RunWith(JUnit4::class)
class SubjectsViewModelTest {
    private lateinit var context: Context


    @Test
    fun addNewSubject_setsNewSubjectEvent() {
//        val context = ApplicationProvider.getApplicationContext<AttendanceManagerApplication>()
//        val viewModel = factory.create(SubjectsViewModel::class.java)
//
//        val observer = Observer<Event<Unit>> {}
//        try {
//            viewModel.newSubjectEvent.observeForever(observer)
//
//            viewModel.addNewSubject()
//
//            val value = viewModel.newSubjectEvent.value
//            assertThat(value?.getContentIfNotHandled(), (not(nullValue())))
//        } finally {
//            viewModel.newSubjectEvent.removeObserver(observer)
//        }

    }
}